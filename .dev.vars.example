# Copy to .dev.vars for local development without Access login.
# Never configure DEV_IDENTITY as a deployed Worker variable; production builds ignore it.
ACCESS_ISSUER=""
ACCESS_AUD=""
DEV_IDENTITY=local-user
