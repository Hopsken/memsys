# Tag list (`list-tags`)

Adds the `list_tags` tool: every `#anchor` in use, lowercase, sorted, without counts. Agents call it before writing so they reuse existing tags instead of inventing near-duplicates. No settings.
